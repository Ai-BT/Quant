"""
실시간 거래 설정

가상 거래(Paper Trading) 설정값들
"""

# ============================================
# 전략 설정
# ============================================

# 이동평균선 설정
FAST_PERIOD = 20      # 단기 이동평균 기간
SLOW_PERIOD = 50      # 장기 이동평균 기간

# RSI 설정
RSI_PERIOD = 14       # RSI 계산 기간
RSI_BUY_THRESHOLD = 50.0   # 매수 시 RSI 최대값 (이 값 이하일 때만 매수)
RSI_SELL_THRESHOLD = 70.0  # 매도 시 RSI 최소값 (이 값 이상일 때만 매도)

# ============================================
# 가상 거래 설정
# ============================================

INITIAL_CASH = 1_000_000  # 초기 자본금 (100만원)
COMMISSION = 0.0005        # 수수료율 (0.05%)

# ============================================
# 거래 설정
# ============================================

MARKET = 'KRW-BTC'    # 거래할 마켓
INTERVAL = 60         # 체크 주기 (초) - 1분마다 체크
CANDLE_MINUTES = 1    # 분봉 단위 (1, 3, 5, 10, 15, 30, 60, 240)
CANDLE_COUNT = 200    # 수집할 캔들 개수 (200개 = 최소 slow_period보다 커야 함)

# ============================================
# 로깅 설정
# ============================================

LOG_DIR = 'logs'      # 로그 파일 디렉토리
SAVE_TRADES = True    # 거래 내역 저장 여부
